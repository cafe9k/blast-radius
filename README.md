# blast-radius
The Agent analyzes the dependency relationships of components in the code repository, and further assesses the blast radius of each component, thereby reflecting the project's engineering complexity and scalability.
